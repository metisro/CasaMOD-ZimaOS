# Optional runtime ports. Recreate both containers after changing them.
ZIMAMOD_DASHBOARD_PORT=8088
ZIMAMOD_API_PORT=8090

# Optional API-token override. When empty, the API generates /data/api-token.txt.
# Override values must contain at least 32 characters.
ZIMAMOD_API_TOKEN=
